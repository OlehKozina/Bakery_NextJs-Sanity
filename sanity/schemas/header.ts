const header = {
  fields: [
    {
      name: "navigation",
      of: [{ type: "link" }],
      type: "array",
    },
  ],
  name: "header",
  title: "Header",
  type: "document",
};

export default header;
